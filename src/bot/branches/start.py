class StartBranch:
    async def state1(self) -> str:
        text = (
            "Hello, it's Bitcoin rate predictor bot!"
            "List of my commands:"
            "1. /predict - get a rate prediction for the next 7 days"
        )
        return text
